'use strict';

function Rating(grade, book, user) {
    this.name = name || '';
    this.book = book || null;
    this.user = user || null;
}

Rating.prototype = {
    name: '',
    book: null,
    user: null
};